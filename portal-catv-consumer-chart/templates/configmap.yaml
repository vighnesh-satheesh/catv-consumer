apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "portal-catv-consumer-chart.fullname" . }}-configmap
  labels:
  {{- include "portal-catv-consumer-chart.labels" . | nindent 4 }}
data:
{{toYaml .Values.configMap | nindent 2}}